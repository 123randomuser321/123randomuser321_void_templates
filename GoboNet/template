# Template file for 'GoboNet'
# generated with xnew
pkgname=GoboNet
version=0.12
revision=1
create_wrksrc=yes
build_style=gnu-configure
configure_args="--prefix=/usr"
hostmakedepends="autoconf automake"
depends="wpa_supplicant dhcpcd util-linux net-tools wireless_tools psmisc pinentry bash coreutils"
# You could also use inetutils-ifconfig vs net-tools but I do not know how to let xbps switch yet
short_desc="An ultra-minimalistic network manager"
maintainer="Sembo Sadur <labmailssadur@gmail.com>"
license="GPL-3.0-or-later"
homepage="https://github.com/gobolinux/GoboNet"
distfiles="https://github.com/gobolinux/GoboNet/archive/${version}.tar.gz"
checksum=12c0e61b69c376d3ace356e2bdba21b1705cccd2f1107b1d3ed07582e86707cd

post_extract() {
	mv ${pkgname}-${version}/* ${wrksrc}
	rm -rf ${pkgname}-${version}
}

pre_configure() {
	./autogen.sh
}
